# Proxy target for the Vite dev server (vite.config.js).
# In production, Nginx handles the /api → backend proxy — this var is dev-only.
# When running outside Docker (local npm run dev) change to http://localhost:5000.
VITE_PROXY_TARGET=http://backend:5000
